<?xml version="1.0" encoding="UTF-8"?><rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
    <channel>
        <title><![CDATA[Changelog]]></title>
        <description><![CDATA[Release notes for Claude Desktop]]></description>
        <link>https://claude.com/docs</link>
        <generator>Mintlify</generator>
        <atom:link href="https://claude.com/docs/cowork/changelog/rss.xml" rel="self" type="application/rss+xml"/>
        <copyright><![CDATA[Claude.ai Documentation]]></copyright>
        <docs>https://claude.com/docs</docs>
        <item>
            <title><![CDATA[v2.2553.0]]></title>
            <link>https://claude.com/docs/cowork/changelog#v2-2553-0</link>
            <guid isPermaLink="false">835c17c82416c905</guid>
            <pubDate>Thu, 17 Sep 2026 18:17:50 GMT</pubDate>
            <content:encoded><![CDATA[<p>Bundled Claude Code version: 2.1.274.</p>
<p><strong>General</strong></p>
<ul>
<li>Changed the sidebar's Pinned list to show its first 20 items, with a Show more link for the rest.</li>
<li>Fixed connector and extension tools in Cowork and Code sessions failing with "expected nonoptional" when Claude left out an optional parameter that has a default.</li>
<li>Fixed plugins past the first 100 in a marketplace not opening from Customize.</li>
<li>Fixed Read Aloud continuing to play the old response after you regenerate, edit, or retry a reply, and cutting off early when a server error ended the read.</li>
<li>Fixed some extensions that use the built-in Node.js runtime stopping with a "Server disconnected" error right after they start.</li>
<li>Fixed the app closing and then failing to reopen on some Windows-on-ARM (Snapdragon) PCs installed from the Microsoft Store package.</li>
<li>Fixed update restarts ending Code tab sessions and MCP servers abruptly instead of shutting them down the way quitting does.</li>
<li>Fixed re-enabled plugins sometimes turning themselves back off, and new sessions sometimes starting without a plugin that was shown as enabled.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>Added file viewing to cloud sessions, so file links in the transcript open in the app, the Files pane can search the session's files, and the Background tasks panel shows background command output.</li>
<li>Fixed <code>!</code> commands and a code block's Run being dropped when the terminal was not ready; they now run within seconds, use the selected terminal tab when it is idle, show as terminal rows when sent while Claude is still working, and no longer leave the session marked as running after their output is sent.</li>
<li>Fixed a case where a session could stop accepting messages after the app relaunched or the computer slept, until the app was restarted.</li>
<li>Fixed a routine created with every connector removed still using all of your connectors.</li>
<li>Fixed new sessions sometimes taking minutes to start while another session's worktree was being cleaned up or recycled.</li>
<li>Fixed Plan mode skipping permission prompts when the Bypass permissions option is on and the session fell back to an older bundled Claude Code CLI while an update downloaded.</li>
<li>Fixed turns failing with "OAuth token has expired" hours into a session; the token is now renewed in the background, and when a fresh sign-in is genuinely required the app asks before sending and keeps your message.</li>
<li>Fixed sessions in large repositories starting slowly, including repositories that use a <code>.worktreeinclude</code> file, and worktrees occasionally starting without their local settings files.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>Fixed plugin skills that reference <code>${CLAUDE_PLUGIN_ROOT}</code> or <code>${CLAUDE_SKILL_DIR}</code> sometimes running their commands with those paths blank.</li>
<li>Fixed preinstalled Node.js packages such as <code>pptxgenjs</code>, <code>docx</code>, <code>pdf-lib</code> and <code>sharp</code> not being found by <code>require()</code>.</li>
<li>Fixed search and session loading crashing on very long conversations.</li>
<li>Fixed stacked permission prompts sliding up under the mouse pointer right after one is answered, which could send the next click to the wrong prompt.</li>
<li>Fixed workspace updates failing with "Not enough disk space" on computers that had enough room once the update replaced the previous version.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>Added <code>allowedPluginMcpServers</code>: when set, Cowork, Chat and Code sessions connect only the plugin MCP servers whose URL matches an entry (an empty list allows none), in addition to <code>managedMcpServers</code> and organization plugins, even when <code>managedMcpServers</code> is empty; the app also stops starting plugin MCP servers on the device, except for organization plugins. The Setup window can now author an empty list for this key and for <code>allowedWorkspaceFolders</code>.</li>
<li>Added <code>builtinBrowserEnabled</code> to offer a built-in browser in Cowork and Code sessions, with <code>builtinBrowserDefaultDomainPolicy</code>, <code>builtinBrowserAllowedDomains</code>, and <code>builtinBrowserBlockedDomains</code> to control which sites Claude may open in it.</li>
<li>Added <code>inferenceCredentialHelperWindows</code>: the absolute path of the credential helper executable on Windows devices, used there instead of <code>inferenceCredentialHelper</code>, so one configuration can serve Windows and macOS or Linux devices.</li>
<li>Added a Salesforce preset to the plugin marketplace picker in the Setup window.</li>
<li>Changed <code>inferenceFoundryResource</code>: a value delivered by a bootstrap URL the user configured themselves (in Settings or a local configuration file) now asks that user to approve it before it takes effect, as the Foundry base URL and the other providers' endpoints already do, and declining quits the app. Because the resource name is required, each such Foundry install prompts once after updating. Values delivered through device management, or by a bootstrap URL that device management set or that <code>trustBootstrapDelivery: true</code> covers, are unchanged and never prompt.</li>
<li>Updated Customize to a full page with redesigned Skills, Plugins and Connectors lists (search, an Add menu, a page per item) in place of the old tables in Settings; Memory moved under Settings.</li>
<li>Fixed a <code>Bash</code> entry in <code>disabledBuiltinTools</code> or <code>builtinToolPolicy</code> not applying to PowerShell commands on Windows PCs without Git for Windows; a <code>Bash(**)</code> entry is now treated as <code>Bash</code>.</li>
<li>Fixed Settings > Desktop app > General on macOS offering a Voice shortcut that could not work; a Caps Lock or custom shortcut chosen earlier no longer opens the dictation bar.</li>
<li>Fixed the app continuously re-fetching its configuration from the configuration server and re-testing the inference connection while the inference provider rejects the configured API key.</li>
<li>Fixed the Code tab's <code>/status</code> card showing a placeholder account; it now shows your signed-in identity and the deployment name.</li>
<li>Fixed the model list, organization configuration, or an MCP server occasionally failing to load right after launch when <code>egressProxyUrl</code> is set and the network is reachable only through that proxy.</li>
<li>Fixed the model picker listing a model twice when a gateway's <code>/v1/models</code> endpoint returns both <code>&#x3C;id></code> and <code>&#x3C;id>[1m]</code>; the pair now shows as the model and its 1M-context row.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v2.110.0]]></title>
            <link>https://claude.com/docs/cowork/changelog#v2-110-0</link>
            <guid isPermaLink="false">68fce05c84a7cb98</guid>
            <pubDate>Tue, 15 Sep 2026 17:19:57 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>Added a <code>chromiumFlags</code> setting in <code>claude_desktop_config.json</code> for GPU-related switches such as <code>--disable-gpu</code>, applied before graphics start up, so users can work around GPU driver crashes and rendering issues on launch, including on Microsoft Store installs where command-line flags cannot be passed.</li>
<li>Changed the error screen shown after repeated crashes or failed starts to say what happened and offer a Restart Claude button, and crash recovery now waits progressively longer between retries instead of retrying immediately.</li>
<li>Fixed a new chat's first message sometimes being sent twice after a reload, failing repeatedly with "Missing files" over an attachment uploaded for an earlier chat attempt, or dropping you back to an empty new-chat page when the first reply was interrupted; also fixed an edited message being sent again each time Enter was pressed in the edit box that stays open after Save.</li>
<li>Fixed the app freezing and then reloading on Windows (Microsoft Store and other MSIX installs) while an update downloaded in the background.</li>
<li>Fixed the app sometimes failing to launch on Windows when its settings file briefly couldn't be opened.</li>
<li>Fixed the Code tab's agent detection, output styles, and other desktop features sometimes needing you to sign in to your Claude account again after a single network or server error while that sign-in was being renewed.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>Changed SSH and WSL sessions to keep running when left idle in the background instead of being stopped, and fixed them stalling for up to 10 minutes after each reply while the desktop app was closed or the computer was asleep.</li>
<li>Removed the calendar view from the Routines page; routines now always show as cards.</li>
<li>Fixed messages sent to an SSH session around a disconnect being dropped, discarded with an error, or answered with a prompt to send them again; the app now checks whether the message arrived and delivers it once the session is restored.</li>
<li>Fixed permission requests and questions not appearing in popped-out windows and split panes after the main window switched away from the Code tab.</li>
<li>Fixed text typed in the prompt box sometimes disappearing, along with its undo history, while a session was open.</li>
<li>Fixed the app freezing on launch or session switch when a very large unsent prompt had been saved; it now comes back as an editable "Saved draft" attachment.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>Fixed browser, computer use, and website access permission prompts in Dispatch sometimes being denied on their own before you could answer.</li>
<li>Fixed connected-folder issues: deleting a file failed with "Could not find mount for path" when two connected folders shared a name, and Claude could see the wrong files in a newly connected folder named <code>.claude</code>.</li>
<li>Fixed Cowork tasks being unable to read or update existing artifacts.</li>
<li>Fixed Cowork startup problems on Windows: some tasks failed to start, a task could appear to start when your drive couldn't be reached (it now stops with a clear error), mapped network drives no longer hold up startup, and projects now load and save on virtual desktops that use profile containers such as FSLogix.</li>
<li>Fixed Reconnect on an enterprise-managed connector doing nothing when your SSO session had expired; it now signs you in with SSO again, and offers signing in with your own account after a failed attempt.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>Added <code>inferenceCredentialHelperArgs</code>: a list of arguments passed in order to the <code>inferenceCredentialHelper</code> script, so one installed script can serve several environments; when unset the script runs with no arguments, as before.</li>
<li>Added <code>inferenceFoundryBaseUrl</code>: routes Azure AI Foundry requests from Chat, Cowork, and Code through a gateway or proxy you run instead of the resource's own endpoint; it takes the same value as Claude Code's <code>ANTHROPIC_FOUNDRY_BASE_URL</code>.</li>
<li>Added <code>redirectHost</code> to <code>bootstrapOidc</code>, to <code>inferenceGatewayOidc</code> (interactive gateway sign-in), and to <code>inferenceVertexWorkforceOidc</code> (Vertex workforce sign-in), so organizations whose identity provider only accepts <code>localhost</code> in a registered redirect URI can complete browser sign-in; the default remains <code>127.0.0.1</code>.</li>
<li>Added <code>scheduledTasksEnabled</code>: set it to <code>false</code> to turn off scheduled tasks in Cowork and Code; the Scheduled page is hidden, existing tasks stop running, and Claude can no longer schedule new work.</li>
<li>Added effort and default-model controls: <code>defaultModelEffort</code> sets the effort level the default model starts at, <code>maxEffort</code> on an <code>inferenceModels</code> entry hides that model's higher effort levels and holds Code sessions to the cap, and <code>alwaysStartWithDefaultModel</code> starts every new conversation or task on the default model and stops saving a person's model and effort changes as their default. The Code tab also now uses the standard model picker, with effort as its own control beside the model name.</li>
<li>Added model catalog support: model names, descriptions, and thinking or effort options in the model picker now follow Anthropic's published Claude Code model catalog, matching what first-party users see, with your configured model list and order unchanged. By default the app fetches the signed catalog from <code>downloads.claude.ai</code> (the host it already uses for workspace and Claude Code downloads) every 5 to 15 minutes and keeps the last catalog it fetched, or the copy bundled with the app, when that host cannot be reached; set <code>modelCatalogEnabled</code> to <code>false</code> to keep the app's built-in labels and make no catalog request, or <code>modelCatalogUrl</code> to fetch the catalog and its signature file from a mirror inside your network instead (the document is still verified against the key built into the app).</li>
<li>Changed Chat to stop asking for approval when Claude hands back a file it produced and, with advanced file analysis on (<code>chatAdvancedFileAnalysisEnabled</code>), at each step of analyzing an attached file, matching Cowork; connector actions still ask. To keep the per-step prompt in Chat and Cowork, add <code>"Bash": "ask"</code> to <code>builtinToolPolicy</code>.</li>
<li>Changed the Cowork workspace's and other native log files on macOS to be written to <code>~/Library/Logs/Claude-3p</code> with the rest of the deployment's logs instead of <code>~/Library/Logs/Claude</code>.</li>
<li>Changed MCP tool permissions: entries in <code>managedMcpServers</code> and <code>orgPluginSettings</code> now apply to MCP servers from any installed plugin, including ones that run locally, a <code>managedMcpServers</code> entry can use <code>transport: "policy-only"</code> to set tool permissions for a server a plugin provides without declaring how to launch it, and permission rules now also apply to tools whose names contain characters such as dots or spaces.</li>
<li>Changed SSH connections in Code sessions on macOS and Linux to run through the OpenSSH <code>ssh</code> program on the device by default, so the organization's own SSH setup (for example Kerberos sign-in and <code>ssh_config</code> options) applies; set <code>sshTransport</code> (beta) to <code>builtin</code> to keep the app's built-in SSH library.</li>
<li>Fixed "Instructions for Claude" in Settings appearing blank right after saving, and profile settings switching to a different saved copy after a launch that asked the user to sign in.</li>
<li>Fixed Duplicate saving an empty copy of a configuration, and Claude API key sign-in overwriting a saved configuration, when the configuration's file could not be read, for example while another program briefly had it locked.</li>
<li>Fixed sign-in guidance when the deployment's credential has no sign-in to repeat: authentication error cards now show that credential's own guidance instead of asking to sign in again, the sign-in card says what failed when sign-in succeeded but the organization's configuration could not be loaded, and a gateway 403 is reported as a provider error instead of a request to re-enter credentials.</li>
<li>Fixed the model picker listing the same model name many times when a gateway returns several models under one display name; the extras now appear under More models, labelled by the part of the model ID that differs.</li>
<li>Fixed scheduled tasks silently not running after the provider sign-in (such as AWS IAM Identity Center) had expired; they now wait and run once you sign in again, and the "Scheduled task failed" notification now appears when a task could not start.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Resolved: Cowork on Windows]]></title>
            <link>https://claude.com/docs/cowork/changelog#resolved-cowork-on-windows</link>
            <guid isPermaLink="false">3662e57e36f08dcb</guid>
            <pubDate>Mon, 14 Sep 2026 19:31:16 GMT</pubDate>
            <content:encoded><![CDATA[<p>Microsoft has released a Windows update that fixes the issue where Cowork could not reach your files on Windows PCs. Install the latest Windows update and restart your PC. On Windows 11 24H2 and 25H2 the fix is <a href="https://support.microsoft.com/en-us/servicing/os/windows-11/2026/09/kb5129195-windows-11-24h2-25h2-security-update">KB5129195</a>. No Claude Desktop update is needed.</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.52386.6]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-52386-6</link>
            <guid isPermaLink="false">96f10fce70f30e41</guid>
            <pubDate>Mon, 14 Sep 2026 00:12:55 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>Updated the bundled Claude Code CLI to version 2.1.270.</li>
<li>Fixed organization plugins enabled through Claude Code's managed settings not loading; they load from the next session.</li>
<li>Fixed sessions with a very large prompt getting permanently stuck on a "Prompt is too long" error.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>Fixed users sometimes being told a model they have access to is restricted, and a running session silently switching to the organization's default model.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.52386.3]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-52386-3</link>
            <guid isPermaLink="false">6506e4159a682521</guid>
            <pubDate>Fri, 11 Sep 2026 19:34:52 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>Changed the automatic move of scheduled tasks to the cloud, for accounts where it has started: the app now waits about a minute after the computer wakes, and while offline checks again each minute, instead of trying at once and then waiting hours after a failed try.</li>
<li>Fixed two problems with local projects that are moving to claude.ai, for accounts where that move has started: a project could refuse new tasks for hours while part of its memory copy waited on the server (it now accepts new tasks and the copy finishes in the background), and memory files shown in a moved project's earlier tasks would not open.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.52386.0]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-52386-0</link>
            <guid isPermaLink="false">cb1f5ab94d303f08</guid>
            <pubDate>Thu, 10 Sep 2026 22:37:27 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>Changed messages sent while Claude is still replying: they now wait in the conversation looking like sent messages, the first waiting message offers Send now, which stops the current reply and sends it next, and Cmd/Ctrl+Enter sends your message right away, interrupting the current turn.</li>
<li>Fixed Claude's clicks, scrolls, screenshots, and page scripts in the built-in browser stalling, timing out, or failing while the browser pane was hidden or the window was minimized or in the background, and fixed elements picked with "Select element" reaching Claude without their styles or with a screenshot of the wrong part of the page.</li>
<li>Fixed links that open a new tab to claude.ai or claude.com pages, like Upgrade buttons and some download links, doing nothing when clicked; they now open in your default browser.</li>
<li>Fixed sites on a private network (VPN, Tailscale, or intranet hosts) loading without their styles, scripts, or data in the built-in browser, and URLs typed into the built-in browser in cloud sessions not reaching private-network sites.</li>
<li>Fixed the app window resetting to its default size and position after an update or a display change.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>Added a default transcript view setting: choose whether new sessions open in the Normal, Thinking, or Verbose view from Settings > Claude Code, or make the current view the default from a session's Transcript view menu. The choice syncs between the desktop app and claude.ai.</li>
<li>Fixed Code sessions failing to start on some Microsoft Store and MSIX installs on Windows.</li>
<li>Fixed model changes being refused with a message saying organization-managed hooks could not be checked, for some organizations that manage Claude Code plugins; the session now restarts on the chosen model.</li>
<li>Fixed sessions failing to start on Windows for accounts with many plugins installed.</li>
<li>Fixed several SSH session issues: hosts slowly accumulating leftover background processes from older Claude versions until sessions there failed, sessions from one computer being ended when a second computer's Claude app cleaned up the same host, sessions permanently losing their connection after a saved connection's host or port was edited, and history missing its newest messages soon after launch or a reconnect.</li>
<li>Fixed WSL sessions failing to start or reconnect, including background reconnects right after an app update, when WSL reported a momentary error.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>Changed what happens on Windows PCs where a Windows update released September 8, 2026 prevents Claude's workspace from reaching your files: the app now names that cause instead of deleting and reinstalling the workspace. This does not fix the underlying problem, and the workspace still can't reach your files on those PCs.</li>
<li>Fixed a session appearing stuck running when an organization hook blocked a message; the reason the message was blocked is now shown.</li>
<li>Fixed an Office file preview sometimes showing "Failed to load PDF document" instead of the spreadsheet or document view.</li>
<li>Fixed long-running tasks failing with an authentication error after the app renewed your sign-in in the background; the running task now picks up the renewed sign-in and continues.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>Added <code>coworkVmIpv6Enabled</code>, which gives the Cowork workspace VM an IPv6 address and route so the agent's tools can reach IPv6-only hosts through the device's own IPv6 connectivity. macOS and Windows; off by default. <code>coworkEgressAllowedHosts</code> still decides which hostnames the tools may reach.</li>
<li>Added <code>sshTransport</code> (beta), which chooses the SSH engine that carries Code sessions: <code>system-openssh</code> runs the OpenSSH <code>ssh</code> program on the device, so connections can use the organization's own SSH setup (for example Kerberos sign-in, including on Windows), and <code>builtin</code> uses the app's built-in SSH library. Unset or <code>auto</code> keeps the build's default.</li>
<li>Added a deprecation notice in the Setup window under any managed-configuration setting that is scheduled to stop being accepted, naming the date and what to use instead.</li>
<li>Added organization-set session retention. <code>chatSessionRetentionDays</code>, <code>coworkSessionRetentionDays</code>, and <code>codeSessionRetentionDays</code> each delete that surface's idle sessions from the device, along with their files, after the set number of days (1 to 3650) without activity; unset deletes nothing. <code>sessionRetentionHold</code> suspends all automatic deletion for the users it is set for, as a legal hold. Projects, Spaces, and memory stay, and a Code session's uncommitted work stays on disk.</li>
<li>Changed app launch to open the home composer on the last-used Chat or Cowork mode instead of always opening Cowork first.</li>
<li>Changed hooks from organization plugins to also run in Chat, matching Cowork and Code.</li>
<li>Deprecated an undocumented client-certificate fallback setting that releases from 1.49585.0 on no longer read, since the app now presents TLS client certificates natively. Deployments that still set it keep working unchanged, but their users see an in-app deprecation warning from November 3, 2026, and the setting stops being accepted on November 17, 2026; remove it once every device is on 1.49585.0 or later.</li>
<li>Improved the connection test against gateways that require a TLS client certificate the device does not have: the result now says the device did not present a certificate the gateway accepts, so the connection could not be tested, instead of reporting the model as rejected.</li>
<li>Fixed Claude Code's own settings on the device (a user's or project's settings file, or a <code>managed-settings.json</code>) being able to turn OpenTelemetry trace export back on after an administrator configured a collector with traces off; with <code>otlpEndpoint</code> set, Cowork and Code sessions now export traces only when <code>otlpTracesEnabled</code> is <code>true</code>.</li>
<li>Fixed Amazon Bedrock and Google Vertex AI sessions retrying a failed request for several minutes when the AWS or Google Cloud credential on the machine had expired; the request now stops after one retry so the credential can be refreshed.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Known issue: Cowork on Windows]]></title>
            <link>https://claude.com/docs/cowork/changelog#known-issue-cowork-on-windows</link>
            <guid isPermaLink="false">7acc34139817438d</guid>
            <pubDate>Mon, 14 Sep 2026 19:31:16 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>Resolved September 14, 2026.</strong> Microsoft has released a Windows update that fixes this issue. Install the latest Windows update and restart your PC.</p>
<p>A Windows update released September 8, 2026 (including KB5124008) stops Cowork from reaching your files when it runs on your Windows PC, so tasks fail or the workspace does not start. This affects Cowork on third-party inference deployments and Cowork sessions that run on your computer rather than in the cloud. Cloud sessions and Claude Code, including the Code tab, are not affected. Chat cloud sessions are not affected, but Chat sessions on third-party inference deployments are affected if using advanced file analysis. The cause is a change in Windows, so restarting or reinstalling Claude does not help.</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.49585.0]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-49585-0</link>
            <guid isPermaLink="false">2f63faea0439e914</guid>
            <pubDate>Tue, 08 Sep 2026 17:32:14 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>Changed an invalid "Required organization" device-policy value to block sign-in with a configuration error, shown in the diagnostic report, instead of being ignored.</li>
<li>Updated the app runtime to Electron 44 (Chromium 152); macOS 13 Ventura or later is now required.</li>
<li>Fixed chats started from the menu bar or Quick Entry panel opening as an empty page for up to a minute before the reply appeared, and ignoring your instructions from Settings > Instructions for Claude.</li>
<li>Fixed macOS sometimes showing repeated "bash would like to access data from other apps" prompts after quitting the app with sessions open.</li>
<li>Fixed scheduled tasks around Mac sleep: a task that ran while the Mac was asleep was sometimes stopped as unresponsive when it woke, tasks missed during sleep sometimes started during a brief background wake and failed (they now start once the Mac is fully awake), and a one-time task occasionally started many sessions at once after wake.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>Improved terminal tabs: they now show when a command is still running and ask before closing one that is, and a tab whose shell crashed or was killed stays open with a Restart option instead of closing silently with its output.</li>
<li>Removed the Git requirement for local sessions that don't use a worktree; on Windows, Git for Windows (Git Bash) is no longer needed to start a session.</li>
<li>Fixed the Files pane discarding unsaved edits without asking when you closed the pane, expanded another pane, or opened it in a new window; fixed the editor joining lines in files that mix Windows and Unix line endings; and a failed save now says the file couldn't be saved and offers Try again instead of claiming the file changed on disk.</li>
<li>Fixed the selected model reverting or being refused when you switched models while a session was starting, restarting, or still answering; the session now uses the model you chose, and model changes in SSH and WSL sessions no longer fail with "plugin hooks could not be loaded".</li>
<li>Added automatic sending for messages held after you hit your 5-hour limit: they now send when the limit resets, and you can still edit, cancel, or send them early.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>Fixed a conversation started from an artifact, or from an artifact comment's Send to Claude, not using your selected model.</li>
<li>Fixed a task disappearing from the app while its files stayed on disk when one of them was in use during delete; the task is now kept so the delete can be retried.</li>
<li>Fixed an issue where the app could quit at launch with a very large number of Cowork tasks.</li>
<li>Fixed the "Can't reach the Claude API" warning staying on screen until the app was restarted even after the connection had recovered; it now clears on its own.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>Added <code>continuousAccessEvaluation</code> to the Microsoft 365 entry in <code>managedMcpServers</code>: the bundled connector's sign-ins, through the OS sign-in broker as well as the browser, request Continuous Access Evaluation tokens from Microsoft, which can live up to about 28 hours but are revoked within minutes when an administrator revokes sessions or a tenant network policy no longer allows them; set it to <code>disabled</code> to keep standard one-hour tokens on every sign-in path. Defaults to <code>enabled</code>.</li>
<li>Added each model's description from the gateway to the model picker for deployments that discover models from the gateway.</li>
<li>Changed <code>microsoftAuthBroker</code>: a new <code>required</code> option makes Microsoft 365 sign-in fail when the OS sign-in broker is unavailable instead of falling back to the browser, so the refresh token always stays held by the broker, and removes the token cache an earlier browser sign-in left on disk. Earlier versions treat <code>required</code> as <code>disabled</code> (browser sign-in only), so set it once every device is on this version or later.</li>
<li>Changed Claude API, Google Vertex AI, Amazon Bedrock, and Bedrock Mantle deployments that set no custom base URL to no longer suppress Claude Code's experimental features, so tool search is on by default there (on Vertex AI with Claude 4.5 and newer models) and <code>toolSearchEnabled</code> is no longer needed to turn it on; gateway and Foundry deployments, and those providers behind a custom base URL, are unchanged.</li>
<li>Fixed Amazon Bedrock sessions that use IAM Identity Center sign-in showing an internal error or a bare "operation was aborted" message when AWS sign-in could not be reached at session start; the app now says whether IAM Identity Center was unreachable, temporarily unavailable, refused the account or role, or returned an unreadable response, and what to do next.</li>
<li>Fixed find in page (⌘F) missing matches in messages scrolled out of view.</li>
<li>Fixed Google Cloud (Vertex AI) sessions sometimes running under the computer's own Google login instead of the credential your organization configured, and the app not asking you to sign in again after your Google session expired.</li>
<li>Fixed the app refreshing the sign-in token about once a second when an inference gateway answers 403 to the model list request.</li>
<li>Fixed the built-in Microsoft 365 connector showing Connected before anyone had signed in; its first request in a conversation now offers the sign-in, and a cancelled sign-in no longer hides its tools; the bundled connector also gains a tool that reads Teams channel messages.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.46388.3]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-46388-3</link>
            <guid isPermaLink="false">8538240c191a8a2b</guid>
            <pubDate>Fri, 04 Sep 2026 16:40:21 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>Added support for attaching your home folder, Windows Documents, AppData, the macOS Library folder, and whole drives; Claude's own configuration and session data inside them stay off-limits, as do certain credential and shell-startup locations (for example SSH keys, AWS and Google Cloud credentials, and bash, zsh and PowerShell profile files).</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.46388.2]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-46388-2</link>
            <guid isPermaLink="false">25547dcf20cbb5c2</guid>
            <pubDate>Fri, 04 Sep 2026 15:34:10 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>Fixed Code sessions started in a git worktree failing to initialize on Windows.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.46388.1]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-46388-1</link>
            <guid isPermaLink="false">91cce7b2d1dfa5fd</guid>
            <pubDate>Fri, 04 Sep 2026 02:08:01 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>Removed the "Mark as unread" action for chats.</li>
<li>Fixed links between artifacts and shared-artifact links clicked inside the app opening in the browser or losing their share key; they now open in place, and a teammate without access sees the request-access screen instead of "not available".</li>
<li>Fixed memory use on macOS growing steadily while working with files, which could end with the system reporting it had run out of application memory.</li>
<li>Fixed new messages being queued instead of sent after reopening a chat whose last message had no reply yet.</li>
<li>Fixed removing a file from the message box after stopping or losing a reply sometimes deleting that file from the message you had already sent, which made "Try again" on it fail.</li>
<li>Fixed side chat, the slash-command picker, usage insights, and other quick actions failing silently on machines where an organization deploys a Claude Code <code>managed-mcp.json</code>.</li>
<li>Fixed the dictation microphone button disappearing from the chat composer once a message had text, an attached file, or a quoted reply.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>Added a queue for messages sent while your 5-hour usage limit is reached: they wait above the composer instead of failing, and you can edit, cancel, or send them when you're ready.</li>
<li>Added keep-awake while Claude works: the computer no longer idle-sleeps while Claude is working in the Code tab, with a "Keep computer awake while Claude works" setting (and a "Keep awake on battery power" option) under Settings > Claude Code, a "Keep computer awake for this session" item in the session menu, and a one-time notice after a long task.</li>
<li>Removed the Summary transcript view from the session's Transcript view menu; sessions that had it selected open in the Normal view.</li>
<li>Fixed Extra high and Max effort on Claude Opus 5 quietly running at High when thinking was turned off in your Claude Code settings; those efforts now run with thinking on for the session.</li>
<li>Fixed organization-managed settings not loading, and Remote Control not connecting automatically, for some users until they signed out and back in.</li>
<li>Fixed sessions failing to open: a "Couldn't load this" message now recovers on its own instead of needing a manual reload, sessions no longer show "No messages yet" on Windows hosts that use FSLogix or similar profile containers, and a transcript that can't be read shows the reason with a Retry button.</li>
<li>Fixed SSH sessions being restarted after some reconnects, which could leave duplicate Claude Code processes on the remote machine, and fixed messages held for an unreachable host: they are retried after an app restart, and when the host comes back needing your password or an unlocked SSH agent the app asks you instead of marking the message "needs you".</li>
<li>Fixed switching models in a session: a switch refused with a "plugin hooks could not be loaded" error now restarts that session's Claude Code on the model you picked, the model picker no longer shows a model Claude Code refused, and Remote Control sessions no longer offer models the connected Claude Code version can't run.</li>
<li>Fixed switching models staying blocked for the rest of the session when an organization-managed plugin's marketplace or a plugin hook failed to load.</li>
<li>Fixed the remembered folder sometimes getting mixed up with a remote machine's path, which made every local session launch fail with "working directory no longer exists".</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>Added automatic re-runs (after 5, 15, and 30 minutes) for a scheduled task that could not reach the model at all, for example right after the computer wakes behind a VPN.</li>
<li>Fixed Record a skill opening an unresponsive chooser window on Windows.</li>
<li>Fixed saving a skill Claude proposes in a conversation: a name that matches one of your skills now offers to update that skill instead of failing with "Try again", and when saving is blocked until you sign in again the Save skill button opens the sign-in prompt instead of a generic error.</li>
<li>Fixed scheduled tasks and other automatic ways of starting a session choosing a model the installed app version can't run, which made every turn fail.</li>
<li>Fixed the row under the composer (Add folder, permission mode, model) disappearing after switching a session to automatic approvals.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>Added <code>blockReadsOutsideWorkingDirectories</code>: restricts Code sessions to reading files inside the session folder and <code>allowedWorkspaceFolders</code>; file tools refuse reads elsewhere, and sandboxed shell commands lose access to the home directory.</li>
<li>Added <code>configRecheckIntervalMinutes</code>: how often a running app re-checks its managed configuration for changes, from 2 to 30 minutes; unset means 10 minutes, where the app previously checked every 30. A served value applies without a restart, and the key can also be set from device management.</li>
<li>Added <code>disableBypassPermissionsMode</code>: removes bypass permissions mode from Code sessions and Cowork tasks, so Claude always follows the configured permission policy.</li>
<li>Added <code>sshClientPath</code> (beta): the absolute path of the OpenSSH program the app runs for SSH sessions; when unset, the app uses the first <code>ssh</code> on the user's PATH.</li>
<li>Added an Edit button to your most recent message in Cowork and Chat sessions, so you can revise and resend it, and fixed "Restart conversation from here" doing nothing in Chat sessions.</li>
<li>Added the restart prompt, escalating to a required restart after <code>relaunchEnforcementHours</code>, when device-managed configuration (the managed plist, Windows policy registry, or Linux managed-settings file) changes while the app is running; previously only configuration changes served from a customer bootstrap endpoint prompted one.</li>
<li>(breaking) Changed <code>relaunchEnforcementHours</code>: in served configuration the key moved from <code>bootstrap.relaunchEnforcementHours</code> to <code>lifecycle.relaunchEnforcementHours</code>; this release no longer reads the old path and earlier versions do not read the new one, so move the value under <code>lifecycle</code> (the flat-format and device-management key name is unchanged); the key can now also be set from device management; and the default window before a required restart is now 24 hours instead of 1. A device-management profile that sets any app-behavior key takes precedence over a served value for this key, so such a profile should set it too.</li>
<li>(breaking) Changed how unreadable Claude Code managed settings are handled: if a device's <code>managed-settings.json</code> file, a drop-in, the device-management plist, or the Windows policy registry value cannot be parsed, Claude Code now refuses to start and names the source, so sessions on that device will not start until that file or value is fixed or removed; previously they ran without those settings. Check that these sources parse on managed devices before rolling out this version.</li>
<li>Changed <code>allowedPluginMarketplaces</code> entries set to <code>auto_install</code> or <code>required</code> without a pinned commit SHA (or without <code>manifestSha256</code> for a <code>url</code> source) to show as available with a configuration warning, instead of being removed from the marketplace list.</li>
<li>Changed Claude Code's <code>allowedMcpServers</code> setting (in its <code>managed-settings.json</code>, not the managed-configuration schema) to govern only servers users add themselves: a server from a <code>managed-mcp.json</code> file that the allowlist used to filter out now loads, and <code>deniedMcpServers</code> is the way to keep it off.</li>
<li>Fixed chat search returning no results; searching finds chats by title and message content again.</li>
<li>Fixed deployments without cloud features, or with the Cowork tab turned off, showing controls that could not work there, including Remote Control, automatic pull requests, the Cloud and Slack session filters, Add marketplace, and Cowork slash commands.</li>
<li>Fixed organization plugins staying installed for users after an administrator removed the plugin's folder from the system <code>org-plugins</code> directory.</li>
<li>Fixed remote MCP connectors whose <code>headersHelper</code> mints the credential continuing to fail for up to several minutes after the server rejected an expired credential; the helper now re-runs immediately and the rejected request is retried once.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.44121.4]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-44121-4</link>
            <guid isPermaLink="false">5f5273fd027ce19d</guid>
            <pubDate>Thu, 03 Sep 2026 02:37:50 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>Added support in Chat for skills from organization-provided plugins, and Chat-only users can now see and manage those plugins under Customize, matching Cowork and Code.</li>
<li>Fixed advanced file analysis in Chat failing with "Workspace unavailable" for users whose organization enables <code>chatAdvancedFileAnalysisEnabled</code> but turns Cowork off with <code>coworkTabEnabled: false</code>.</li>
<li>Fixed managed OAuth connectors showing a connection error instead of prompting to sign in when the app starts without a usable sign-in (expired with no refresh token, or never signed in on this machine), including servers added by URL alone, where Connect could fail instead of opening the sign-in page.</li>
<li>Fixed organization-configured URL plugin marketplaces failing to install or load plugins with "marketplace entry path does not stay inside the marketplace directory".</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.44121.2]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-44121-2</link>
            <guid isPermaLink="false">cbc42841dfbee34d</guid>
            <pubDate>Wed, 02 Sep 2026 20:39:19 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>Fixed sessions on Windows being unable to run commands or fetch web pages. This reverts the 1.44121.1 fix for sessions failing to start on Windows for accounts with many saved artifacts, scheduled tasks, or connected folders.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>No user-facing changes.</li>
</ul>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[v1.44121.1]]></title>
            <link>https://claude.com/docs/cowork/changelog#v1-44121-1</link>
            <guid isPermaLink="false">22ab5c09988bcfa9</guid>
            <pubDate>Wed, 02 Sep 2026 15:39:26 GMT</pubDate>
            <content:encoded><![CDATA[<p><strong>General</strong></p>
<ul>
<li>Added Claude to the "Open with" menu for common work files, including spreadsheets, PDFs, Word and PowerPoint documents, text files, and images, on macOS and on Windows (Microsoft Store and MSIX installs).</li>
<li>Fixed a crash at launch when the app's settings file couldn't be read.</li>
<li>Fixed a new chat's first message being dropped when you had to sign in again or verify this device; the chat now asks for that step in a dialog over your message.</li>
<li>Fixed an issue where an invalid settings file could cause all app settings to be reset.</li>
<li>Fixed Claude not finding files attached in Cowork and Chat, including pasted files and a re-attached file with the same name; attachments that can't be read now say so instead of being dropped silently.</li>
<li>Fixed scheduled tasks failing with an error after a permission approval.</li>
<li>Fixed the app reloading endlessly when it keeps crashing right after loading; it now stops and shows what to do next.</li>
</ul>
<p><strong>Code</strong></p>
<ul>
<li>Added a Split View submenu to the View menu for opening a new session beside or below the current one, and side panes (diff, terminal, plan, preview, and others) can now open in a window of their own.</li>
<li>Added Claude Code output styles: an "Output style" submenu in the session menu and a <code>/output-style</code> command for viewing and switching styles, a "New style…" option that drafts a custom style from a plain-language description, and a default style setting in Settings › Claude Code.</li>
<li>Changed the Files pane to open files as tabs beside a collapsible file tree, with single-click preview tabs, multi-select, richer right-click menus, and inline previews for PDF, Word, Excel, and PowerPoint files.</li>
<li>Fixed <code>/rewind</code> appearing undone when returning to a session after navigating away, which could also cause the next message to silently drop recent conversation history.</li>
<li>Fixed Code sessions repeatedly failing to start after a corrupt Claude Code download on macOS, failing to start on Linux arm64, and SSH and WSL session setup failing on slow connections; the one-time install now shows progress and can fall back to uploading Claude Code from your computer.</li>
<li>Fixed sessions failing to start on macOS 12 (Monterey).</li>
<li>Fixed sessions getting stuck on "responding" when a message was queued near the end of a turn.</li>
</ul>
<p><strong>Cowork</strong></p>
<ul>
<li>Changed live artifact sharing to follow your organization's Artifacts setting and sharing policies: members no longer see sharing options their organization has turned off, and already-shared artifacts keep Copy link and Unshare.</li>
<li>Changed updating a skill from a file card or skill proposal to apply in one step with an Undo toast instead of a confirmation dialog; where a confirmation still appears, its Update button is the pre-selected action.</li>
<li>Changed what happens when web fetch isn't available for your organization: Claude now says so instead of retrying and reporting failed fetches.</li>
<li>Fixed a crash when previewing an image file that is empty or actually contains text.</li>
<li>Fixed a message with an attachment that couldn't be read or was too large failing to send over and over with no explanation; Claude now marks the attachment with the problem, says what to do, and offers Retry where it can help.</li>
<li>Fixed sessions failing to start on Windows for accounts with many saved artifacts, scheduled tasks, or connected folders.</li>
<li>Fixed sessions that run Claude Code directly on the device failing to start for organizations that set <code>disableSideloadFlags</code> in Claude Code's managed settings (<code>managed-settings.json</code>, an MDM profile, or the registry); in those sessions Claude Code loads none of the desktop's plugins, so their commands, agents, and hooks are unavailable.</li>
<li>Fixed the session page reloading after you connect a connector that signs in through the browser.</li>
</ul>
<p><strong>3P</strong></p>
<ul>
<li>Added <code>claudeAiImport.automatic3pImport</code> (beta): when <code>true</code> and <code>deploymentOrganizationUuid</code> is set, the app copies this computer's earlier third-party sessions stored before an organization ID was configured into that organization's session store, once per device and in the background, independently of <code>claudeAiImport.enabled</code>; a copy interrupted when the app quits resumes on the next launch.</li>
<li>Added <code>egressProxyUrl</code> and <code>egressProxyPacUrl</code>: route the app's and the agent's traffic through a corporate HTTP proxy, or let a PAC file choose the proxy per request, instead of following the operating system's proxy settings; on macOS and Windows, Cowork's workspace follows the pinned proxy too. Both keys are read from device management or the local configuration file only, and the PAC file wins when both are set.</li>
<li>Added <code>inferenceStreamIdleTimeoutSec</code>: how many extra seconds (300 to 1800, default 300) Chat, Cowork, and Code sessions wait for model output on a streaming response that is sending only keep-alive pings, for gateways that send keep-alives while the upstream model is silent. Gateway provider only.</li>
<li>Added a Duplicates step to the import wizard when an imported Project has the same name as one you already have, with the option to merge the imported sessions into your Project and remove the duplicate; the Projects page offers the same merge.</li>
<li>Added conversation titles to the OpenTelemetry export: a <code>desktop_session_title_set</code> event carries each Cowork and Code session's title plus the Claude Code session ID to join on, sent when <code>otlpDesktopLogLevel</code> is <code>info</code> or lower; the title text is included only when <code>otlpContentCapture</code> includes <code>userPrompts</code>.</li>
<li>Added installing and updating plugins from admin-configured marketplaces on devices where Cowork isn't available, from the Code tab or the Cowork tab.</li>
<li>Added local scheduled tasks to the Code tab, including the <code>/schedule</code> command and the Scheduled page, matching what Cowork already offered.</li>
<li>Added the ability to attach files in SSH Code sessions.</li>
<li>(breaking) Changed the Usage page cost estimate to require <code>inferenceModelPricingEnabled</code>; <code>inferenceModelPricingMultiplier</code> and <code>inferenceModelPricing</code> now refine the estimate only while it is on and no longer turn it on by themselves.</li>
<li>Changed <code>HTTPS_PROXY</code>, <code>HTTP_PROXY</code>, and <code>NO_PROXY</code> set in the <code>env</code> block of Claude Code's managed settings (<code>managed-settings.json</code> or your organization's server-managed Claude Code settings) to apply to Chat, Cowork, and Code sessions even when the computer has a system proxy configured.</li>
<li>Changed <code>isClaudeCodeForDesktopEnabled</code>: when it is <code>false</code>, the app no longer starts Code sessions even if asked directly, Preview no longer scans projects for a dev server, and the computer is no longer offered for Remote Control; a Code session requested anyway shows "Code sessions are turned off by your organization" instead of a retry prompt.</li>
<li>Fixed Bedrock and Bedrock Mantle sessions being cut off by network idle timeouts during long thinking phases on Opus 4.7 and later models.</li>
<li>Fixed Code tab sessions not prompting to re-authenticate when the deployment's credentials had expired; they now show the same sign-in prompt as Cowork. Also fixed Live Artifact <code>askClaude()</code> calls and the Code tab's "Detect dev server" failing on deployments that use SSO, credential helpers, Vertex, or Bedrock SSO.</li>
<li>Fixed managed MCP connector sign-in staying permanently stuck when the identity provider no longer recognizes the OAuth client the app registered; the app now registers a new client automatically.</li>
<li>Fixed MCP servers provided by plugins not connecting in Code and Cowork sessions when <code>managedMcpServers</code> is configured; in that case, with <code>isLocalDevMcpEnabled</code> set to <code>false</code>, plugins' remote MCP servers connect and their local (stdio) ones stay blocked.</li>
<li>Fixed plugins distributed as a zip whose top level is a single component folder (for example <code>skills/</code> or <code>commands/</code>) installing with no skills, commands, or agents.</li>
<li>Fixed the <code>allowedWorkspaceFolders</code> policy not always being applied to Code sessions over SSH.</li>
<li>Fixed the Code tab home's usage stats reporting a favorite model, per-model breakdown, and activity from other Claude Code history on the same machine instead of this deployment's own sessions.</li>
</ul>]]></content:encoded>
        </item>
    </channel>
</rss>